/*

// Simple Types
let group = 'CNUG';
let age:number;

// Interfaces (Person)
interface Person {
    name: string;
}

// Classes (Greeter)
class Greeter {
    constructor(private message: string) {
    }

    greet(person: Person) {
        console.log(this.message + ' ' + person.name);
    }
}

let greeter = new Greeter('Hello');
greeter.greet({ name: 'Rasmus' });

// JSX
<p>Hello CNUG</p>

*/